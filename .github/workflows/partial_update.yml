name: Partial Update Programs

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # Optional: Run daily at midnight UTC

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r program/requirements.txt

      - name: Run update script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          cd program
          python process_programs.py partial

      - name: Commit and push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add program/metadata.json program/data/*.txt
          git commit -m "Partial update of programs data [skip ci]" || echo "No changes to commit"
          git push
